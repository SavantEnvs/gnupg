project: savantenvs/gnupg
target: fuzz_list

# Starter seeds: OpenPGP sample data shipped in gnupg's own tests/openpgp/ tree, copied under
# mayhem/fuzz_list/testsuite/. Two-entry form: the server-accumulated tar (so the corpus grows across
# runs) plus the per-target local seeds.
testsuite:
  - https://$MAYHEM_DOMAIN/$MAYHEM_PROJECT/$MAYHEM_TARGET/testsuite.tar
  - file://mayhem/fuzz_list/testsuite

cmds:
  - cmd: /mayhem/fuzz_list
    libfuzzer: true
